
namespace Rtps.Structure.Types
{

    /// <summary>
    /// Topic Kind, to distinguish between single instance and multiple instaces topic
    /// </summary>
    public enum TopicKind : int
    {
        NO_KEY = 1,
        WITH_KEY = 2
    }
}
